spring:
  application:
    name: backend
  mongodb:
    uri: ${SPRING_MONGODB_URI:mongodb://localhost:27017/dsm}

app:
  jwt:
    secret: local-development-secret-key-change-before-production
    expiration: 86400000
